{
  "id" : "${element.id}",
  "name" : "${element.name}",
  "patterns" : [
    ${generate element.patterns-safe pattern_element.json}
    "pattern_end"
  ],
  "since" : "${element.version}",
  "description" : "${element.desc-safe}",
  "examples" : "${element.examples-safe}"
},
